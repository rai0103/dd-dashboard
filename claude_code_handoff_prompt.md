# DD戦略ダッシュボード — Claude Code 引き継ぎプロンプト

## このファイルの使い方
Claude Codeのチャットに、このファイル全体をそのまま貼り付けて開発を依頼してください。
「まず技術構成を提案してから、段階的に実装して」と付け加えると進めやすいです。
`dd_dashboard_mockup.jsx`（UI/UXの決定版モックアップ）も同じフォルダに置いた上で、一緒に読み込ませてください。

---

## 0. 位置づけ

- 元の機能要件・デフォルト値・確率テーブル等は、後半に貼り付けた「DD戦略ダッシュボード — Web開発プロンプト」が正本です。
- UI/UX（レイアウト・配色・インタラクション・データ入力フロー）は `dd_dashboard_mockup.jsx` が決定版です。チャット上でのやり取りを経て、当初の仕様書から以下の点が更新・具体化されています。実装時は元の仕様書よりモックアップの内容を優先してください。

### モックアップで決定済みのUI仕様（元仕様書からの更新点）

- **画面構成（PC）**: 深度ゲージ（左端縦帯・DDを視覚的な「深度」として表現するシグネチャー要素）＋ 上部ステータスパネル ＋ 2×2グリッド（左上：チャート、右上：AIアドバイス、左下：ポートフォリオ構成、右下：A〜E配分乖離）
- **配色**: ダーク基調（背景 #0A0F1C）。ティール(#45C4B0・浅い/安全)→琥珀(#D9A24B・中間)→レンガ(#C0654B・深い/警戒)のグラデーションでDD水準を表現
- **節目(milestone)リスト**: `-3/-5/-8/-10/-12/-15/-18/-20/-25/-30/-35/-40/-45/-50%/-50%以上`（元仕様書の8段階から拡張）
- **チャート**: 評価額（左軸）とDD%（右軸）を1枚に重ね表示、横軸は時間軸。凡例クリックで系列の表示/非表示切替。期間セレクター（1/3/6ヶ月・年初来・1/3/5/10/20/30年・最長）
- **暴落比較タブ**: チャートに「通常表示／暴落比較」のタブを追加。ブラックマンデー(1987)・ドットコムバブル崩壊(2000-02)・リーマンショック(2007-09)・コロナショック(2020)を「下落開始からの経過日数」軸で正規化し、現在のDD推移と重ねて比較。各暴落をクリックすると全画面で詳細（期間・原因・終息要因・学び、現状との同一経過日数比較）を表示
- **確率パネル**: メイン画面には「その後の下落確率」の要約のみ表示し、クリックで全画面のトラックレコード（進行確率・最終到達確率・速度条件付き確率）を表示。最終到達確率には発生頻度（例：「年に1.9回」「2.2年に1度」）を併記。発生頻度はDD3%級の押し目が年3.5回発生する仮定からの逆算値（実データ接続後は実測値に置き換え）
- **ポートフォリオ構成**: 円グラフ＋凡例。カテゴリー別／為替別／A〜Eランク別を切替可能。クリックで全画面の構成表（列見出しクリックでソート可能）
- **A〜E配分乖離**: 各行(A〜E)クリックでそのランクの保有銘柄・金額・口座種別・口座主を全画面表示。ヘッダーの(i)アイコンでDD毎の全12段階配分表を全画面表示
- **AIアドバイス**: 現状のDD水準の発生頻度、さらに下落する確率、推奨アクション、取り崩し・現金バッファの目安を表示
- **データ入力**（ヘッダーの「データ入力」ボタン→モーダル）:
  - **VOO価格データ**: 方式A＝StooqのCSV取り込み、方式B＝日次終値の直接入力
  - **VOO上場前（2010-09-07より前）の補完**: SP500長期CSVをアップロードすると、VOOの初値と同時期のSP500水準から換算係数を計算し、上場前の期間を自動合成してVOOデータに継ぎ足す
  - **保有資産データ**: 楽天証券の残高CSV（Shift-JIS）を取り込み、銘柄名からカテゴリー／A〜Eランク／為替を自動推定。取り込み前にプレビュー画面で確認・修正可能。**修正内容は銘柄名ごとに記憶し、次回以降の取り込みで自動適用**（都度直す必要がない）
  - 保有資産CSVは口座主（shin/saki）単位で取り込み、既存データのうち同じ口座主の分だけを置き換える

---

## 1. 追加要件（このチャットで確定）

**利用シーン**: 基本はPCで使う個人用ツールだが、スマホでも「その日の終値だけサッと入力」「外出先で状況確認」ができるようにしたい。別アプリは作らず、以下の1本化で対応する。

### レスポンシブ対応（必須）
- 同一コードベース・同一データで、画面幅に応じてレイアウトを切替
- PC: 現行の2×2グリッド
- スマホ: ステータス→チャート→ポートフォリオ→A〜E配分→アドバイスの順で縦カード積み（元仕様書⑩の通り）
- Tailwindのブレークポイント(`md:`等)、または`window.innerWidth`での分岐で実装

### PWA化
- `manifest.json` と簡易Service Workerを追加し、スマホのホーム画面に追加してアプリのように起動できるようにする
- オフライン時のキャッシュ戦略は最小限（静的アセットのキャッシュ程度）でよい

### データ同期（PC⇄スマホ）
- 現状の `window.storage`（Claudeアーティファクト専用API）はブラウザ・端末ごとに独立しているため、**そのままではPCとスマホでデータが共有されない**
- 採用方式: **Cloudflare Worker + プライベートGitHubリポジトリ**（seijika-map.comの証拠投稿機能=`politician-map-issues` Workerと同じパターンを流用）
  - VOO日次データ、保有資産データ、銘柄名分類の修正履歴（overrides）を、WorkerがGitHub Issues APIまたはリポジトリ内のJSONファイルとして読み書き
  - フロントエンドはこのWorker経由でデータをGET/POSTし、`window.storage`／`IndexedDB`はあくまでローカルキャッシュとして併用
  - 認証は既存の運用に合わせて簡易なもの（個人利用のため）でよいが、公開リポジトリに資産情報を平文で置かないよう注意
- 代替案（採用しない場合の候補）: Firebase(Firestore)。同期・オフライン対応を自作したくない場合の選択肢として提示のみ

### スマホ画面での優先度
- スマホでの主用途は「その日の終値を1件入力」なので、直接入力フォームを最も手前（開いてすぐ）に出す導線にする

---

## 2. 実装時の注意点（このチャットで判明した差異）

- `window.storage`（get/set/delete/list、shared=false）はClaudeアーティファクト専用APIです。実運用では**IndexedDB＋上記Cloudflare Worker同期**に置き換えてください
- 楽天証券CSVの実フォーマット（列名・区切り文字・ヘッダー行位置）は未検証です。モックアップのパーサーは「銘柄」「口座」「評価額」を含む見出し行を探す汎用ロジックのため、実ファイルで調整が必要になる可能性が高いです
- VOO上場前データの合成（SP500からの換算）は、配当再投資を含まない価格指数としての概算です。VOOの実際の値動きと完全には一致しません
- 暴落比較タブの過去暴落カーブは、開始日・底値日・最大DD・回復日という実績に近い数値から生成した**簡易近似曲線**です。実際の日次値動きではないため、精度が必要な場合は実際のhistorical drawdownデータへの差し替えを検討してください
- 過去確率テーブルのうち、`-12/-18/-25/-35/-40/-45%` の値は元データ（1957-2026・69年トラックレコード）に基づく実測値ではなく、既存の実測値から補間した暫定値です（UI上も「参考値」ラベルで区別済み）

---

## 3. 開発の進め方（元仕様書ベース、モックアップの構成に合わせて微修正）

1. 技術構成を確定（React単体構成のままGitHub Pagesに置くか、レスポンシブ・PWA対応を踏まえてNext.js化するか等）し、`dd_dashboard_mockup.jsx` を土台にPC/スマホ双方のワイヤーフレームを合意
2. チャート＋DD表示、確率テーブル（トラックレコードモーダル）を先に実データ対応
3. VOO価格データ入力（CSV→SP500補完→直接入力の順）
4. 保有資産データ入力（楽天CSV取り込み＋A〜E自動分類＋修正記憶）
5. ポートフォリオ構成・A〜E配分乖離の連動確認
6. 暴落比較タブ、DD毎の配分表モーダル
7. レスポンシブ対応・PWA化
8. Cloudflare Worker + GitHubによるPC⇄スマホ同期
9. AIアドバイス（Anthropic API連携、キー管理の設計とセット）

各段階で実際に動くものを作り、GitHub Pagesで動作確認してから次に進めてください（一度に全部作らない）。

---

---

# 【正本】DD戦略ダッシュボード — Web開発プロンプト（元仕様書）

## このドキュメントの使い方
別チャット(Claude Code 推奨)で、このドキュメント全体を貼り付けて開発を依頼する。
「まず要件を理解し、技術構成を提案してから、段階的に実装して」と指示すると進めやすい。

---

## 1. サービスの目的

投資家(個人・50歳・取り崩しフェーズ・所得ゼロ・世帯資産約1億円)が、
自ら確立した「ドローダウン(DD)連動リバランス戦略」を、
**毎日の運用で客観的・機械的に判断するための可視化ダッシュボード**。

核心思想:
- 感情(高値でのビビり早売り／上昇相場での油断した強気)を排し、**DD水準と過去確率で機械的に判断**する
- VOO(S&P500)のATHからのDD%を「モードスイッチ」として、攻守を切り替える
- 「VOOフルインベストに勝る」を最低ライン、上昇はDD3%まで伸ばし、下落は守り、大底で現金を投入して反攻する

---

## 2. 機能要件(10項目)

### ① SP500チャート + DDチャート表示
- S&P500(またはVOO)の価格チャートと、そのATHからのDD%チャートを上下2段で表示
- DDチャートには節目ライン(-3/-5/-8/-10/-15/-20/-30%)を水平線で表示
- 現在のDD水準と「モード」(最高値更新モード/下落モード)を明示
- ATH更新をリセット点とする(DD3%発生後はATH完全回復までモード固定)

**VOO価格データの入力方式(2択、ユーザーが選択)**
API連携はしない(公開時のCORS・キー問題を避けるため)。以下の2方式を切替可能にする。

- **方式A: StooqのCSV読み込み**
  - ユーザーがStooqからダウンロードしたVOOのCSVをアップロード(ファイル選択)
  - 取得元: `https://stooq.com/q/d/?s=voo.us`(ページ下部の「Download data in .csv file」)
  - または直接DL: `https://stooq.com/q/d/l/?s=voo.us&i=d`(日次)
  - **CSV形式**: ヘッダー行 `Date,Open,High,Low,Close,Volume`、日付は `YYYY-MM-DD`、昇順(古い→新しい)
    - 例: `2026-08-27,703.10,707.50,701.20,704.20,3500000`
  - パース時は `Close` 列のみ使えばDD計算は可能(OHLCVすべて読み込んでもよい)
  - アップロードしたCSVはブラウザ内に保持し、次回起動時も使えるようにする(IndexedDB)
  - 差分更新: 既存データの最終日以降だけ新CSVで追記する運用も想定(まずは全置換でよい)
  - 初期バンドル: アプリに1957-2026のS&P500日次データ(またはVOO)を同梱し、初回から動く状態にする

- **方式B: 数値の直接入力**
  - 毎日の運用で、その日のVOO終値だけを手入力するシンプルな方式
  - 入力欄: 日付(デフォルト今日)+ 終値。「追加」でその日の値を時系列に足す
  - 入力履歴はブラウザ内に保持(IndexedDB)、時系列としてチャート・DD計算に使う
  - ATH・DD・モードは、入力された終値系列から自動再計算
  - 最小運用: 毎朝この1数値を入れるだけで、モード判定と確率テーブルが更新される

- **共通**: どちらの方式でも、内部的には「日付+終値の時系列」に正規化して扱う。
  ATH = 系列の累積最大、DD% = 現在値/ATH - 1。方式Aで過去分を入れ、方式Bで日々追記、という併用も可能にすると理想的。

### ② 保有資産のインプット(3方式)
- **スクリーンショット**: 証券口座画面の画像をアップ → OCR/画像解析で銘柄・評価額を抽出
- **CSV**: 証券会社の残高CSV(楽天証券・moomoo等)をアップ → パース
  - 注意: Shift-JISエンコード、1行目に「■資産合計」等のヘッダー行がある形式に対応
  - 列: 銘柄名・口座区分(特定/NISA成長/NISAつみたて)・平均取得・現在値・評価額
- **直接入力**: フォームで銘柄・数量・取得単価・口座を手入力
- 入力データはブラウザ内(localStorage不可の場合はIndexedDBまたはメモリ)で保持

### ③ A〜E分類 と モデル配分との差異表示
- 保有銘柄を自動で A〜E に分類(分類ルールは後述、手動修正も可能)
- 現在のDD水準に対応する「モデル配分(A〜E目標%)」を表示
- **現状 vs モデル の差異**を、分類ごとに数値+バーで可視化
- 差異が大きい分類を強調(例: 「C(SP500)が-25%不足」)
- ブロック集計(A+B / C / D+E)も表示

### ④ リターン目標に応じたAI戦略提案
- ユーザーが「〇年で〇倍」を設定(デフォルト: 5年で2倍)
- 過去のトラックレコード(バックテスト)をベースに、目標達成に必要なCAGR・配分・リスクを提示
- 達成確率、必要なレバレッジ水準、想定される最大DDを併記
- 目標が非現実的な場合は正直に伝える(例: 「5年で3倍はレバ必須で暴落耐性が犠牲に」)

### ⑤ デフォルト設定 + 全項目再調整可能
- 初期状態は「これまでの議論に基づくデフォルト値」(後述)をセット
- A〜E目標配分、DD各段階の配分、コアギア比率、トレイル幅、リターン目標、追加/取り崩し額 — すべて後から調整可能
- 調整はUIから直感的に(スライダー・数値入力)

### ⑥ DD節目ごとの過去確率テーブル
- 「今DD〇%。この先どこまで下げるか」を過去データ(1957-2026, 69年)から表で提供
- 2種類の確率を表示:
  - **最終到達確率**: DD3%到達時、最終的にDD5/8/10/15/20/30%まで行く確率
  - **進行確率**: 今の節目から次の節目へ進む確率
- **経過日数(速度)による条件付き**も表示: 「DD3→5%が5日以内(急落)なら本格下落確率34%、6日以上(緩慢)なら13%」
- 客観的な投資判断材料として、感情を排する目的

### ⑦ カテゴリー別・通貨別の保有割合可視化
- カテゴリー: SP500 / NASDAQ / 日本株 / ゴールド / 現金 / その他 の割合(円グラフ or 積み上げバー)
- 通貨: 円 / ドル / その他 の割合
- A〜E分類とは別軸の「資産の中身」ビュー

### ⑧ 追加投資・取り崩しの反映
- 年間の追加投資額 / 取り崩し額を入力(デフォルト: 取り崩し年480万)
- 取り崩しフェーズかどうかで、現金バッファ推奨額(生活費2-3年分)を計算・表示
- ライフステージ(例: 配偶者就労中→退職予定)を考慮した現金比率の推奨
- アドバイスに反映(例: 「退職1-2年前から高値圏で現金比率を上げる準備を」)

### ⑨ PC: 1画面ダッシュボード
- PCブラウザでは1画面に主要情報を集約
- 各項目をクリックすると詳細/調整パネルが展開(ドリルダウン)
- レイアウト例: 左にチャート、右にA〜E差異、下に確率テーブルと配分

### ⑩ スマホ: 縦スクロール
- スマホでは項目ごとにカード化して縦スクロール
- チャート → A〜E差異 → 確率テーブル → 配分 → アドバイス の順
- レスポンシブ対応必須

---

## 3. デフォルト設定値(これまでの議論に基づく)

### A〜E分類の定義
| 分類 | 内容 | 代表銘柄 |
|---|---|---|
| A | 現金・高配当・金分 | 現金/HDV/2013/399A日経高配当/ゴールドプラスの金分 |
| B | 金・ヘッジ・防衛 | GLD/2521円ヘッジSP500/ITA防衛 |
| C | SP500 | VOO/eMAXIS SP500/楽天SP500/SPY |
| D | NASDAQ/テック指数 | QQQ/NASDAQ100/FANG+/Zテック/テック指数/日本株の一部 |
| E | レバ・モメンタム・個別 | SPXL/SPUU/SOXL/MSFU/METU/個別株 |

### A〜E目標配分(DD連動12段階)
DD水準に応じて配分を変える。各行合計100%。

| 局面(VOO DD) | A | B | C | D | E |
|---|---:|---:|---:|---:|---:|
| +15% | 12 | 12 | 36 | 22 | 18 |
| +10% | 13 | 12 | 37 | 22 | 16 |
| +5% | 14 | 13 | 38 | 20 | 15 |
| ATH(基準) | 15 | 13 | 40 | 20 | 12 |
| DD-3% | 14 | 13 | 41 | 20 | 12 |
| DD-5% | 15 | 14 | 42 | 19 | 10 |
| DD-8% | 16 | 15 | 42 | 18 | 9 |
| DD-10% | 17 | 16 | 42 | 17 | 8 |
| DD-15% | 16 | 16 | 42 | 18 | 8 |
| DD-20% | 14 | 15 | 40 | 19 | 12 |
| DD-30% | 10 | 12 | 38 | 22 | 18 |
| DD-50% | 6 | 9 | 33 | 25 | 27 |

### コアギア(C+E内のSP500エクスポージャー: VOO/SPUU/SPXL 比率)
| 局面 | VOO | SPUU | SPXL |
|---|---:|---:|---:|
| +15% | 55 | 30 | 15 |
| ATH | 65 | 25 | 10 |
| DD-3% | 72 | 20 | 8 |
| DD-5% | 85 | 12 | 3 |
| DD-10% | 95 | 5 | 0 |
| DD-15〜20% | 100 | 0 | 0 |
| DD-30% | 85 | 10 | 5 |
| DD-50% | 60 | 22 | 18 |

### トレイル(個別・セクター、VOOと無関係)
| 対象 | トレイル幅 |
|---|---|
| SOXL/TECL | -25% |
| レバ2倍(MSFU/METU) | -20% |
| SPXL | -22% |
| FANG+ | -16% |
| テック指数 | -15% |
| 日経225 | -10% |

### DD節目ごとの過去確率(1957-2026, 69年, S&P500)
**最終到達確率(DD3%到達を100%とする)**
| 最終到達 | 確率 |
|---|---:|
| DD-5% | 55% |
| DD-8% | 28% |
| DD-10% | 20% |
| DD-15% | 13% |
| DD-20% | 10% |
| DD-30% | 5% |

**進行確率(その節目から次へ)**
| 遷移 | 確率 |
|---|---:|
| DD-3%→5% | 55% |
| DD-5%→8% | 52% |
| DD-8%→10% | 72%(分水嶺) |
| DD-10%→15% | 65% |
| DD-15%→20% | 73% |
| DD-20%→30% | 55% |

**速度による条件付き(DD5%到達時、DD3→5%の日数別に、最終的な深さ)**
| | →DD8% | →DD10% | →DD15% | →DD20% |
|---|---:|---:|---:|---:|
| 急落(5日以内) | 56% | 44% | 34% | 25% |
| 緩慢(6日以上) | 47% | 30% | 13% | 10% |

**重要な解釈(UIに注記として表示)**
- DD3%到達後の最終深度: 中央値-5.3%(典型は浅い)、平均-9.1%(稀な大暴落が引き上げる)
- DD8%が「浅い調整」と「本格下落」の分水嶺(超えると72%で深化)
- 速度は入り口(DD3→5%)で最も有効、深部では節目の進行確率が主役
- これらは傾向であり確定予測ではない(補助指標として使う)

### リターン目標(デフォルト)
- 5年で2倍(必要CAGR 約14.9%)

### 追加投資・取り崩し(デフォルト)
- 取り崩し: 年480万円
- 現金バッファ目安: 生活費2-3年分(約1,000-1,440万)
- ライフステージ: 配偶者就労中(将来退職予定)→ 退職が近づいたら現金比率を段階的に引き上げ

---

## 4. コアロジック(実装すべき計算)

### DD計算とモード判定
```
ATH = これまでの最高値(価格系列の累積最大)
DD% = (現在値 / ATH - 1) × 100
モード:
  - DDが-3%に達したら「下落モード」に入り、そのATHを固定基準にする
  - ATHを完全回復するまで「上昇」を考慮しない(早売り防止)
  - ATH更新で「最高値更新モード」に戻り、基準を更新
```

### 上昇基準の定義
- 上昇基準 = 前回ATHから3%DD後に回復した点(押し目ごとにリセット)
- DD3%がモード切替スイッチ

### A〜E分類の自動判定
- 銘柄名から分類(キーワードマッチ + 手動修正可能な対応表)
- ゴールドプラス系は「株分」と「金分」に分割(金分→A or B、株分→C or D)

### 差異計算
```
各分類の差異 = 現状% - モデル%(現在のDD水準に対応する行)
ブロック差異 = (A+B), C, (D+E) それぞれで集計
```

### DD3%到達時の「一気に全部やらない」ロジック
- DD3%は1段階移行スイッチであり、全組み替えではない
- DD3%でやること: ①レバを5%枠に刈る ②マクロ配分を1段階移行 ③コアギア1段ダウン
- 構造的乖離(C不足・D過剰等)は別レイヤーで、時間をかけて漸進是正する旨を表示

---

## 5. 技術構成の指針

- **フロントエンド**: React(単一ページ)。チャートは Recharts か Chart.js
- **データ**:
  - S&P500/VOOの価格データ: **API連携はしない**。初期はバンドルしたCSV(1957-2026)で動かし、更新は「StooqのCSVアップロード」または「終値の直接入力」の2方式(機能①参照)
  - Stooq CSV形式: `Date,Open,High,Low,Close,Volume`(日付 YYYY-MM-DD、昇順)。パースは PapaParse 等でよい
  - 直接入力: 日付+終値を時系列に追記、IndexedDBに保持
  - 過去確率テーブルは事前計算した定数として持つ(このドキュメントの値を使用)
- **状態管理**: React state。設定はブラウザ内保存(IndexedDB推奨、localStorageはartifact制約に注意)
- **画像OCR**: スクショ解析は Tesseract.js かクラウドOCR、または「Claude in Claude」(Anthropic API)で画像から銘柄・金額を構造化抽出(JSON出力を指示)
- **AI提案(機能④)**: Anthropic API(claude-sonnet系)を呼び、保有状況+目標+過去データを渡して戦略提案をJSON構造で受け取る
  - システムプロンプトで「このDD戦略のルールブック」を与え、逸脱しない提案をさせる
- **レスポンシブ**: PCは1画面ダッシュボード+ドリルダウン、スマホは縦スクロールのカード

### GitHub Pages公開を想定
- ユーザーはGitHub(rai0103)でGitHub Pagesに公開する運用
- 単一HTML/JSにビルドできる構成が望ましい(依存を減らす)
- APIキーが必要な機能(AI提案・OCR)は、キーの扱いに注意(公開リポジトリに置かない。ユーザーが自分のキーを入力する方式か、ローカル利用前提を明記)

---

## 6. 免責・安全性(必ず実装)

- 本サービスは投資助言ではなく、ユーザー自身が確立した戦略の「可視化・判断補助ツール」である旨を明記
- 過去確率は将来を保証しない、傾向であり確定予測ではない旨を随所に注記
- 「下落を買う」戦略は回復を前提とする。回復しない下落(構造的崩壊)もありうる旨を注記
- 特定銘柄の売買推奨はしない(あくまで配分の差異と客観データの提示)

---

## 7. 開発の進め方(提案)

1. まず技術構成を確定し、画面ワイヤーフレーム(PC 1画面 / スマホ縦)を合意
2. ①チャート+DD表示 と ⑥確率テーブル(定数ベース)を先に作る(価値が高く依存が少ない)
3. ②入力(まず直接入力→CSV→スクショの順)
4. ③A〜E分類と差異表示
5. ⑦カテゴリー・通貨可視化
6. ⑧追加/取り崩し反映、⑤再調整UI
7. ④AI提案(API連携)は最後(キー管理の設計とセット)
8. ⑨⑩レスポンシブ仕上げ

各段階で動作するものを作り、段階的に機能を足す(一度に全部作らない)。
